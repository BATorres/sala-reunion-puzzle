# import Sala, UsuarioSala, Usuario, Diagrama, DiagramaUsuario, SalaSubscriptionPayload from './generated/prisma-client/prisma.graphql'

type Query {
    findAllSalas: [Sala]
    findAllUsuarios(idUsuario: ID!): [Usuario]
    findOneSala(idSala: ID): Sala!
    findOneUsuario(idUsuario: ID): Usuario!
    buscarUsuariosEnSala(idSala: ID): [UsuarioSala]
    verificarUsuarioEnSala(idUsuario: ID): UsuarioSala
}

type Mutation {
    crearSala(nombre: String!): Sala!
    crearUsuario(nombre: String!, esAdmin: Boolean): Usuario!
    unirseSala(idSala: ID!, idUsuario: ID!): UsuarioSala!
}
